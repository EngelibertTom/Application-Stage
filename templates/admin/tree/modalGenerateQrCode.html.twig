<div class="row">
    <div class="col-md-12">
        <div class="p-3">
            {{ form_start(form) }}
                <div class="row">
                    <div class="col-md-3">
                        {{ form_row(form.nbQrCode) }}
                    </div>
                    <div class="col-md-3">
                        {{ form_row(form.nursery) }}
                    </div>
                    <div class="col-md-3">
                        {{ form_row(form.lot) }}
                    </div>
                    <div class="col-md-3">
                        {{ form_row(form.save) }}
                    </div>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
