{% extends 'admin/base.html.twig' %}
{% import "admin/macros/modals.html.twig" as modals %}

{% set title = 'Accès refusé' %}

{% block body %}

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">

                    <a href="{{ back }}">
                        <i class="material-icons">keyboard_backspace</i> Retour
                    </a>

                    <div class="alert alert-danger mt-3" role="alert">
                        <strong> Accès refusé ! </strong> {{ message | default('Vous n\'avez pas l\'autorisation') }}
                    </div>

                </div>
            </div>
        </div>
    </div>
{% endblock %}
