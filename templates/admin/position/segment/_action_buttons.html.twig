{% import "admin/macros/modals.html.twig" as modals %}

<a href="{{ path('app_admin_segment_edit', { id: segment.id }) }}" rel="tooltip" class="btn btn-success btn-simple">
    <i class="material-icons">edit</i>
</a>
<button type="button" rel="tooltip" class="btn btn-danger btn-simple" data-toggle="modal" data-target="#confirmDelete{{ segment.id }}">
    <i class="material-icons">close</i>
</button>

{{ modals.confirmDelete({
    id: 'confirmDelete' ~ segment.id,
    pathDelete: path('app_admin_segment_delete' , { id: segment.id })
}) }}
