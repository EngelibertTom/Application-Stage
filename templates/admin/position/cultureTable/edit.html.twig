{% extends 'admin/base.html.twig' %}

{% block breadcrumb %}
    <li class="breadcrumb-item" aria-current="page"><a href="{{ path('app_admin_greenhouse_list') }}"> Positions </a>
    </li>
    <li class="breadcrumb-item" aria-current="page"><a href="{{ path('app_admin_culturetable_list') }}"> Tables </a>
    </li>
    <li class="active breadcrumb-item" aria-current="page"> Nouveau </li>
{% endblock %}

{% set nbGreenHouse = (nbGreenHouse is defined and nbGreenHouse) or nbGreenHouse is not defined %}

{% block body %}
    {% if nbGreenHouse %}
        <div class="row">
            <div class="col-md-12">

                <div class="card">
                    <div class="card-header card-header-info">
                        <h4 class="card-title">
                            <i class="material-icons">create</i>
                            Ajouter une nouvelle table
                        </h4>
                    </div>
                    <div class="card-body p-3">

                        <div class="row">
                            <div class="col-md-12 mb-4">
                                <a href="{{ path('app_admin_culturetable_list') }}">
                                    <i class="material-icons">keyboard_backspace</i> Retour à la liste
                                </a>
                            </div>
                        </div>

                        {{ form_start(form) }}

                        <div class="row">
                            <div class="col-md-4">
                                {{ form_row(form.name) }}
                            </div>
                        </div>

                        {{ form_end(form) }}

                    </div>
                </div>
            </div>
        </div>
    {% else %}
        {% include 'admin/position/_includes/_not_found_greenhouse.html.twig' %}
    {% endif %}
{% endblock %}
