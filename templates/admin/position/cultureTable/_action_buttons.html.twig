{% import "admin/macros/modals.html.twig" as modals %}

<a href="{{ path('app_admin_culturetable_edit', { id: cultureTable.id }) }}" rel="tooltip" class="btn btn-success btn-simple">
    <i class="material-icons">edit</i>
</a>
<button type="button" rel="tooltip" class="btn btn-danger btn-simple" data-toggle="modal" data-target="#confirmDelete{{ cultureTable.id }}">
    <i class="material-icons">close</i>
</button>

{{ modals.confirmDelete({
    id: 'confirmDelete' ~ cultureTable.id,
    pathDelete: path('app_admin_culturetable_delete' , { id: cultureTable.id })
}) }}
